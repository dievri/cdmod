version: "3"
services:
    server:
        build: . 
        ports:
            - "30000:30000/udp"
        volumes:
            - "~/.minetest/:/root/.minetest/:rw"
        command: ["--worldname", "cdworld", "--port", "30000"]
